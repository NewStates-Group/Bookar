worker_processes auto;

events {
    worker_connections 4096;
    multi_accept on;
}

http {
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;

    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    upstream backend_stream {
        least_conn;

        server backend:8000;

        keepalive 32;
    }

    upstream frontend_stream {
        server frontend:3000;
    }

    upstream flower_stream {
        server flower:5555;
    }

    server {
        listen 80;
        server_name bookar.study api.bookar.study flower.bookar.study;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl;
        server_name api.bookar.study api.localhost;
        ssl_certificate /etc/letsencrypt/live/api.bookar.study/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/api.bookar.study/privkey.pem;
        location / {
            proxy_pass http://backend_stream;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_connect_timeout 5s;
            proxy_read_timeout 60s;
        }

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }
    }

    server {
        listen 443 ssl;
        server_name flower.bookar.study flower.localhost;
        ssl_certificate /etc/letsencrypt/live/flower.bookar.study/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/flower.bookar.study/privkey.pem;
        location / {
            proxy_pass http://flower_stream;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }
    }

    server {
        listen 443 ssl;
        server_name bookar.study localhost;
        ssl_certificate /etc/letsencrypt/live/bookar.study/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/bookar.study/privkey.pem;
        location / {
            proxy_pass http://frontend_stream;

            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";

            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }
    }
}